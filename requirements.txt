# Root-Mask-and-Skeletons runtime dependencies (curated, latest stable).
# Regenerated during the v3 hardening pass: replaces the prior 113-package
# full-environment freeze (which carried dev/test bloat: jupyter, wandb, ipython,
# GitPython, sentry, pipreqs, etc.) with the actual direct runtime imports.
# Transitive deps (Flask, Werkzeug, narwhals, etc.) are resolved by pip.
#
# Dev/test tooling lives in requirements-dev.txt.
#
# ---------------------------------------------------------------------------
# PyTorch (GPU): NOT pinned here on purpose. The working environment uses a
# CUDA 12.8 build:
#     torch==2.8.0.dev20250325+cu128
#     torchvision==0.22.0.dev20250326+cu128
# Install it from the matching CUDA index BEFORE the packages below, e.g.:
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# Pinning torch in this file would let pip pull a CPU wheel and silently disable
# GPU inference. Keep it managed separately.
# ---------------------------------------------------------------------------

# --- GUI ---
PyQt6==6.11.0
PyQt6-WebEngine==6.11.0

# --- Imaging / scientific ---
numpy==2.2.6
scipy==1.15.3
scikit-image==0.25.2
opencv-python==4.13.0.92
pillow==12.2.0

# --- Data ---
pandas==2.3.3

# --- Visualization (Dash pulls Flask/Werkzeug/plotly transitively) ---
dash==4.1.0
dash-bootstrap-components==2.0.4
plotly==6.7.0

# --- Networking ---
requests==2.34.2

# --- Windows integration (PyInstaller builds, win shell) ---
pywin32==308; sys_platform == "win32"
